server:
  servlet:
    context-path: /
  port: 8081

management:
  server:
    port: 8000
  endpoints:
    web:
      base-path: /

logging:
  level:
    root: INFO
    org.springframework: INFO
    ru.gur.arch: DEBUG
