server:
  servlet:
    context-path: /api/v1
  port: 8080

management:
  server:
    port: 9090
  endpoints:
    web:
      base-path: /

spring:
  application:
    name: archHomework
  main:
    banner-mode: off

logging:
  level:
    root: ERROR
    org.springframework: ERROR
    ru.gur.arch: INFO
